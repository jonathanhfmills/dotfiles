spec_version: "0.1.0"
name: dotfiles
version: 0.1.0
description: Gitclaw agent for dotfiles
model:
  preferred: "openai:gpt-4o-mini"
  fallback: []
tools: [cli, read, write, memory]
runtime:
  max_turns: 50
