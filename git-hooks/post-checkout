#!/bin/bash
DOTFILES="$HOME/dotfiles"
LOG="$DOTFILES/.git/agent.log"
echo "[$(date -Iseconds)] post-checkout triggered" >> "$LOG"
$DOTFILES/opensandbox/.venv/bin/python "$DOTFILES/opensandbox/launch-agent.py" --event post-checkout >> "$LOG" 2>&1 &
