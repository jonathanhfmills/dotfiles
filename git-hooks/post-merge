#!/bin/bash
DOTFILES="$HOME/dotfiles"
LOG="$DOTFILES/.git/agent.log"
echo "[$(date -Iseconds)] post-merge triggered" >> "$LOG"
python3 "$DOTFILES/opensandbox/launch-agent.py" --event post-merge >> "$LOG" 2>&1 &
