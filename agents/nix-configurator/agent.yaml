spec_version: "0.1.0"
name: nix-configurator
version: "1.0.0"
description: >
  NixOS file editor sub-agent. Knows home-manager patterns, anti-patterns, and the
  correct file for every type of change. Reads before editing. Keeps changes minimal.
tags:
  - nix
  - home-manager
  - sub-agent
