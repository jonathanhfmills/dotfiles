# INFERRED.md — Autonomous Architecture Discovery

> This file was generated by qwen-code (Qwen3.5-9B via SGLang) without explicit
> prompting about the Brain/Engineer/Grunt architecture. The agent independently
> inferred the fleet topology, training pipeline, and per-business learning model
> from contextual clues in the codebase.
>
> Preserved as Hermes teacher seeding material — demonstrates emergent
> understanding that can bootstrap the Brain's initial knowledge.

## What qwen-code figured out on its own

### Fleet Topology
- **Brain (Hermes/Wanda)**: Bottleneck detection, GEPA routing, Atropos RL training
- **Engineer (Qwen-Agent/Cosmo)**: Native ATIC tool calling (SGLang, Qwen3.5 canonical)
- **Grunts (NullClaw)**: Instant ClawHub execution, per-client LoRA adapters
- **Google ADK**: Replaces legacy OpenSandbox configuration

### Operational Flow
```
Nanodispatch → Experiment → Bench → CSPO → Production
       ↓
    Google ADK (OpenSandbox)
       ↓
NullClaw Execution
       ↓
GSPO Training → Better next iteration
```

### Architecture Insights
- Per-Business LoRA adapters. Client codebase + conventions + domain.
- Frontier API costs decrease as local capability increases. Flywheel compounds.
- NixOS-native: Declarative, version-controlled, rollbackable.
- Client ISO from GitHub Pages. Private repo. Install and learn day one.

### Self-Modification Awareness
- "Every file is yours — SOUL, AGENTS, MEMORY, memory/. Compounds as you learn."
- Agent understood it can modify its own configuration files
- Recognized the growth/learning loop: daily notes → MEMORY.md → SOUL.md refinement

## Training Signal Value

This demonstrates that a 9B model can infer complex multi-agent architecture from:
1. Nix service definitions (sglang.nix, agent-runner.nix, etc.)
2. Package structures (pkgs/acp-bridge/, pkgs/hermes-agent/, etc.)
3. Directory conventions (agents/coder/, agents/deployer/, etc.)
4. Git history and commit messages

The gap between what was explicitly taught (nothing about Brain/Engineer/Grunt)
and what was inferred (complete topology) is exactly the training signal that
makes this valuable for bootstrapping Hermes.
