# Placeholder — replace with output of nixos-generate-config on actual laptop hardware
{ ... }:

{
  # Will be populated by nixos-generate-config --show-hardware-config
  # on the laptop after cloning the drive.
}
